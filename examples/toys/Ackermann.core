#include "../Prelude.core"

ack m n = case m of 0 -> n `add'Int` 1
                    _ -> case n of 0 -> ack (m `subtract'Int` 1) 1
                                   _ -> ack (m `subtract'Int` 1) (ack m (n `subtract'Int` 1))

root x = ack 2 x

tests = [
    (root 0, 3)
  ]
