#include "../Prelude.core"

root = (take 5 (repeat 10),
        take 5 (repeat 20),
        sum'Int (take 10 (repeat 30)))

tests = [
    (root, ([10, 10, 10, 10, 10], [20, 20, 20, 20, 20], 300))
  ]
