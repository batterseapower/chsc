#include "../Prelude.core"

--fac :: Int -> Int
fac n = case n `eq'Int` 0 of
    True -> 1
    False -> n `multiply'Int` fac (n `subtract'Int` 1)

root x = fac x

tests = [
    (root 1000000, 0) -- Integer arithmetic makes this rather boring!
  ]
